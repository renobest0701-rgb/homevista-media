// FFmpeg processor stub — real FFmpeg is not available in serverless environments.
// Set VIDEO_PROCESSOR="mock" (default) to use MockVideoProcessor instead.
export { MockVideoProcessor as FFmpegProcessor } from "./mock";
